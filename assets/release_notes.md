# Dokkaebi 릴리즈 노트

## v0.5.0 (2026-05-04)

### 새로운 기능
- **`git: view commit` 액션**: 커맨드 팔레트에서 SHA·태그·브랜치 등 git ref 를 입력하면 해당 커밋의 details/diff 를 패널로 즉시 열기 가능
- **`git config commit.template` 자동 적용**: 저장소 commit 템플릿이 설정되어 있으면 비어 있는 commit 메시지에 자동 채워져 커밋 작성 컨텍스트 그대로 시작
- **DeepSeek V4 Pro/Flash 모델 추가**: 1M 토큰 컨텍스트 + thinking/reasoning_effort 지원으로 대형 리팩터링·복잡 추론 작업에서 활용 가능
- **DeepSeek V3 Chat/Reasoner 모델 단종 + 기본값 V4 Pro 로 변경**: 모델 ID 가 `deepseek-chat`/`deepseek-reasoner` → `deepseek-v4-flash`/`deepseek-v4-pro` 로 교체
- **`zed --wait --diff <left> <right>` 단일 diff 종료 동작**: diff 탭만 닫아도 cli 가 종료. 디렉터리·파일 동시에 안 받았을 때 워크스페이스 종료까지 기다리지 않음
- **윈도우 컨텍스트 메뉴 + 탭 바에 "새 중앙 터미널"**: 우클릭 메뉴와 + 메뉴에서 중앙 패널로 직접 터미널을 열 수 있음
- **마크다운 우클릭 메뉴 "링크 복사"**: 마크다운 미리보기·Agent 응답의 링크 위에서 우클릭 시 URL 복사 항목 노출
- **OpenAI compatible 모델 `interleaved_reasoning` 옵션**: thinking 토큰을 본문이 아닌 `reasoning_content` 필드로 분리해 송신, 해당 형식을 요구하는 모델 호환
- **command_aliases 자동완성**: 사용자 settings.json 의 `command_aliases` 값으로 액션 이름을 입력할 때 자동완성·문서·deprecation 메타 노출
- **Welcome 탭 `Ctrl-N` 키바인딩**: 시작 화면에서 새 파일을 즉시 만들 수 있음
- **Copilot/Codestral 메뉴에 "Configure Providers"**: 다른 edit prediction provider 와 일관된 설정 진입점 제공
- **Claude Code 서브에이전트 뷰 → 터미널 점프 버튼**: 서브에이전트 탭 헤더 우측 아이콘 클릭으로 호출 터미널 탭을 즉시 활성화
- **Claude Code MCP 입력 폼 알림**: MCP 서버가 사용자 입력을 요구하는 시점에도 워크스페이스 알림이 발화되어 작업 정체 방지

### UI/UX 개선
- **git panel 섹션 헤더 체크박스**: 그룹 단위로 한 번에 stage/unstage 가능. 호버로 표시되며 부분 체크 상태도 시각적으로 구분
- **inline blame popover 잔상 차단**: 모달이 열리거나 스크롤·커서 이동 시 popover 가 즉시 사라져 화면에 떠 있던 잔상 제거
- **blame popover 빈 본문 차단**: 마크다운 파싱 중인 동안에는 popover 를 띄우지 않아 빈 박스가 잠깐 보이던 현상 해소
- **`Format Selections` 메뉴 가용성 인식**: 활성 LSP/포매터가 range formatting 을 지원할 때만 컨텍스트 메뉴와 키바인딩에 노출
- **Inline Assist 아이콘 자동 숨김**: 진단 패널 toolbar 의 Inline Assist 버튼이 Agent disabled 일 때 사라져 불필요한 아이콘 노출 차단
- **Vim `/` 가 Agent panel 포커스 빼앗지 않음**: Agent 패널 포커스 상태에서 `/` 입력해도 vim search 가 center pane 으로 점프하지 않음
- **Python LSP 가 uv/Poetry/PDM 워크스페이스 root 인식**: 서브 패키지의 `pyproject.toml` 이 아닌 `*.lock` 이 있는 최상위 디렉터리를 워크스페이스 root 로 사용
- **JSON schema 다운로드 proxy 적용**: `http_client.proxy` 설정이 JSON LSP 의 schema 다운로드 요청에도 자동 전달
- **AI 이미지 토큰 추산 정확도 향상**: 다운스케일된 이미지의 토큰 수를 다운스케일 후 실제 크기 기준으로 산출
- **Copilot reasoning 효력 반영**: 이전에는 무시되던 thinking effort 값이 Copilot Chat 요청에 실제 적용되어 모델 응답 깊이 조절 가능
- **Copilot `xhigh` reasoning 라벨 표시**: `xhigh` 값이 "Extra High" 로 UI 에 노출
- **Mistral 도구 호출 id 처리 안정화**: 스트리밍 chunk 에서 보내오는 `"null"` id 무시로 도구 호출이 정상 누적
- **OpenAI provider Responses API 기본 사용**: chat completions 가 아닌 Responses 엔드포인트가 기본 경로가 되어 reasoning 모델 응답이 일관되게 처리
- **`onboarding` 스크롤바 위치 보정**: 스크롤바가 콘텐츠 컨테이너가 아닌 패널 우측 가장자리에 떠 본문 표시 영역과 충돌하지 않음
- **Recent Projects 단축키 라벨 정렬**: 빈 영역의 "Add Local Project" 버튼이 keybinding 과 양 끝 정렬로 표시
- **빈 프로젝트 패널에서 새 파일 생성 가능**: worktree 가 비어 있던 상태에서도 root 가 자동 등록되어 신규 파일 생성 동작
- **Git Graph 행 위치 정확도**: 분수 ui_font_size 에서 canvas 와 table 행이 어긋나 스크롤 시 line/dot 이 row 와 분리되던 문제 해소
- **branch picker Create/Confirm 버튼 활성화**: 두 버튼이 같은 element id 로 충돌해 클릭이 안 되던 문제 fix
- **commit details 단복수 표기**: "1 Changed Files" → "1 Changed File"
- **CLAUDE.md/AGENTS.md 규칙 파일 안정 로드**: worktree 초기 스캔이 끝날 때까지 기다린 뒤 규칙 파일을 읽어 첫 진입에서도 룰이 누락되지 않음
- **외부 디렉터리 symlink 변경 감지**: worktree 의 symlink 가 가리키는 외부 디렉터리에서 발생한 파일 변경도 즉시 인식되어 reload 트리거
- **sticky header 가 현재 row 가리지 않음**: jump-to-definition·위 화살표 등에서 자동 스크롤이 sticky 헤더 영역을 고려해 보고 있던 줄을 가리지 않게 정렬
- **Agent/MCP/cli 가 OS 기본 셸 사용**: `terminal.shell` 설정 대신 시스템 셸을 직접 호출해 tmux session 안에서도 hang 없이 동작
- **메모장 우클릭 메뉴에서 선택 없이도 터미널 전송 가능**: 선택 텍스트가 없을 때도 터미널 항목이 노출되며 클릭 시 메모장 전체 내용을 앞·뒤 공백 제거 후 해당 터미널에 입력
- **마크다운 미리보기 헤딩 크기 계층 정상화**: H1~H6 가 사이즈 차이로 명확히 구분되고 헤딩 사이 상단 여백이 추가되어 가독성 개선
- **One Dark `link_text` 이탤릭 표시**: 마크다운/주석 등의 링크 텍스트가 본문과 시각적으로 구분되어 식별 용이
- **Claude Code 플러그인 재설치 필요 안내**: 본체 업데이트로 플러그인 hook 정의가 바뀌었을 때 설정 페이지에서 안내 문구가 자동 노출되어 새 hook 적용 시점을 놓치지 않음

### 보안
- **rustls-webpki DoS 패치 적용**: 잘못된 인증서 폐기 목록(CRL) 입력으로 panic 을 유도할 수 있던 취약점(GHSA-82j2-j2ch-gfr8) 해소
- **레이아웃 엔진 의존성 grid 패치 적용**: 정수 오버플로우로 안전 API 가 미정의 동작에 도달할 수 있던 취약점(GHSA-38c5-483c-4qqp) 해소. 상류 Zed 와 동일한 taffy 0.10.1 / grid 1.0.1 로 동기화

### 정리
- **`soft_wrap` 설정 단순화**: `preferred_line_length` 값을 사용하던 사용자 설정은 자동으로 `bounded` 로 인식 (호환 유지)
- **미사용 개발자 도구 제거**: UI 인스펙터·컴포넌트 갤러리·스토리북·시작 시간 프로파일러·BasedPyright 배너 정리로 빌드 면적 축소
- **`--system-specs` CLI 플래그 제거**: 사용 빈도가 없던 시스템 사양 출력 옵션 정리. 기존 출력 동작은 동일 정보를 외부 도구로 확인 가능
- **Claude Code 알림 플러그인 hook 발화 빈도 감소**: 모든 도구 호출 대신 서브에이전트(Agent) 호출 시점에만 발화하도록 matcher 명시. 기존 사용자는 플러그인 재설치 권장

### 버그 수정
- **split diff 패닉 회피**: diff 가 동시 편집으로 사라지는 race condition 에서도 panic 대신 빈 결과로 안전 종료
- **extension `download_file` GzipTar 다운로드 실패 회피**: 큰 tar.gz 익스텐션 업데이트 시 `archive header` 오류 없이 받기 가능
- **`Cmd/Ctrl-E` 로 선택한 텍스트로 검색**: 검색바를 열거나 검색을 실행하지 않고 선택만 검색 쿼리로 설정 (편집기·터미널·로그 뷰 등 모든 검색 가능 패널)
- **vim 검색이 커서 단어 폴백 일관성**: `editor_cursor_word` 보조 경로가 SearchableItem 단일 진입점으로 통합되어 동작 차이 해소
- **터미널 종료 시 자식 프로세스 정리 (Unix)**: 셸 종료 후에도 살아남던 자식 프로세스가 SIGTERM 동기 송신으로 즉시 정리 (Windows 영향 없음)
- **Agent 메뉴 토글 후 Alt-Shift-L Rules 단축키 일관 동작**: 옵션 메뉴를 토글한 뒤에도 Rules 단축키가 매번 동일하게 인식
- **outline 패널 pin/unpin 툴팁 즉시 갱신**: 핀 버튼 토글 후 툴팁이 stale 텍스트를 표시하지 않고 현재 상태에 맞게 즉시 업데이트
- **Helix `vgl` 가 줄바꿈 미포함**: 비주얼 모드에서 `vgl` 로 줄 끝까지 선택해도 newline 까지 삼키지 않아 다음 줄까지 의도치 않게 영향 미치지 않음
- **터미널 인라인 어시스턴트 `Cmd/Ctrl-Enter` 즉시 실행**: SecondaryConfirm 이 인식되어 생성된 명령을 바로 실행 모드로 전송
- **Windows 에서 extension manifest 경로 정규화**: extension.toml 의 themes/languages/icon_themes 경로에 `\` 가 포함돼도 WSL 등 원격 환경 업로드 시 인식 가능
- **vim visual paste 시 시스템 클립보드 보존**: 비주얼 선택 위에 `Ctrl-V` 로 paste 해도 선택 텍스트가 시스템 클립보드를 덮어쓰지 않음
- **edit prediction 이 자동완성 메뉴 오염하지 않음**: 한 글자 입력만으로 무관한 유니코드 스니펫이 메뉴에 떠오르던 현상 차단(strong-prefix 매치 도입)
- **새 줄 입력 시 자동 들여쓰기 trailing whitespace 미잔존**: 들여쓰기만 있는 줄에서 Enter 를 연달아 눌러도 빈 줄에 공백이 남지 않음
- **마크다운 미리보기 포커스 시 outline panel 유지**: 미리보기 창으로 포커스 이동 후에도 outline 패널이 활성 에디터 헤딩 트리를 계속 표시
- **Dev Container suggestion 알림에 프로젝트명 + 경로 툴팁**: 알림에 워크트리 이름이 표시되고 마우스 hover 시 전체 경로가 툴팁으로 노출되어 어떤 프로젝트인지 즉시 식별
- **outline 패널 footer 의 토글 단축키 라벨 케이싱 통일**: 다른 키바인딩 표시와 동일한 KeyBinding 컴포넌트로 일관된 스타일·간격 적용
- **탭 바 ←/→ 버튼이 모든 탭에서 동작**: 서브에이전트 뷰·터미널 등 에디터가 아닌 탭 사이 이동도 추적되어 이전/다음 탭으로 즉시 복귀 가능
- **Claude Code 작업 완료 알림 표시 안정화**: 다른 탭에서 Claude 탭으로 포커스를 옮겨도 녹색 완료 아이콘이 유지되어 결과를 확인하기 전에 사라지지 않음

---

## v0.4.8 (2026-04-30)

### UI/UX 개선
- **JS/TS 아웃라인 패널에 중첩 객체 메서드 표시**: 중첩 객체 리터럴 안의 메서드도 아웃라인에 노출되어 깊은 구조의 코드도 한눈에 탐색 가능
- **`.mlx` 파일 OCaml 아이콘**: `.mlx` 확장자가 OCaml 아이콘으로 표시
- **터미널 24비트 컬러 정확도 향상**: 어두운 배경 위 빨강·파랑처럼 인지 명도가 낮은 24비트 RGB 색상도 본래 의도한 색상 그대로 표시 (대비 보정으로 색이 빠지던 현상 해소)
- **터미널 커서가 비활성 창에서 다른 모양으로 표시**: 막대(beam)/밑줄(underline) 커서도 포커스 잃었을 때 hollow 형태로 시각 구분
- **자동완성에서 입력한 대소문자에 맞는 항목 우선**: 동률일 때 `a` 입력 시 `abc`, `A` 입력 시 `ABC` 가 위에 정렬
- **터미널 hyperlink 가 도구 출력 패턴 인식**: `Update(path)`, `Write(path)` 같은 Claude Code 출력의 괄호 안 경로도 클릭으로 열기 가능
- **Vim `cs` 연산자가 동일 줄에 따옴표 여러 개 있을 때 정확히 동작**: `I'm 'good'` 같은 줄에서 cs 가 짝을 잘못 매칭하던 문제 해소
- **마크다운 hover popover 잔상 제거**: 호버가 끝나기 전에 popover 표시 task 가 진행 중이었을 때 종료 후에 잠깐 떠오르던 현상 차단
- **Multi buffer 탭 툴팁 개선**: singleton 이 아닌 multi buffer 도 의미 있는 제목을 툴팁으로 표시
- **분할 에디터에서 진단 밑줄 표시**: split 에디터에서도 inline 진단(밑줄)이 사라지고 gutter 진단 아이콘만 남도록 동작 정렬
- **transparent 테마에서 sticky scroll 헤더 뒤 비침 차단**: sticky 헤더 영역 아래만 마스크해 본문 스크롤이 헤더 위로 비치지 않음
- **hint 진단 색상 분리**: hint 레벨 진단이 info 색이 아닌 hint 전용 색으로 표시
- **`AcceptEditPrediction` 모디파이어로 코드 액션 메뉴가 안 사라짐**: 다른 컨텍스트 메뉴가 활성 상태면 edit prediction 미리보기를 건너뛰어 액션 메뉴가 그대로 유지

### 버그 수정
- **Windows 모든 창 닫은 후 dokkaebi.exe 가 정상 종료**: aws-lc atexit / TLS destructor / loader lock 사이의 데드락으로 백그라운드 프로세스가 남던 문제 수정
- **Windows Alt-Tab 후 Alt 모디파이어 stuck 해소**: 포커스를 다시 얻을 때 modifier 추적 상태가 리셋되어 stale Alt 가 다음 키 입력에 영향 주지 않음
- **Windows 하위 폴더 git diff 빈 화면 수정**: 서브디렉터리 worktree 에서 git diff 가 비어 보이던 문제 해소
- **Windows 에서 Node.js LSP 가 사용자 데이터 디렉터리로 시작 실패하던 문제 수정**: `\\?\` extended-length prefix 가 외부 프로세스 인자에서 제거되어 LSP 정상 시작
- **세션 복원 시 extension 언어 서버 미시작 수정**: 세션 복원 후 늦게 로드된 extension LSP 도 이미 열려 있는 버퍼에 자동 등록
- **inlay hint 가 인접 위치에서 정확한 좌표로 navigation**: inlay 좌우 어디로 붙여야 할지 bias 기반으로 결정해 클릭/이동이 어색한 점프 없이 동작
- **edit prediction 자동완성 메뉴 오염 수정**: `show_in_completions_menu` 비활성 시 unicode 같은 무관한 snippet 이 자동완성 메뉴에 끼지 않음
- **저장 안 된 버퍼의 gutter play 버튼 표시 안 함**: 디스크에 없는 새 파일에서 클릭해도 동작 안 하던 play 버튼이 처음부터 안 나옴
- **읽기 전용 의존성 디렉터리가 Save Dialog 기본 진입 경로에서 제외**: `.venv`, `node_modules` 등 `read_only_files` 패턴에 매치되는 경로는 "최근 활성 경로" 후보에서 빠짐
- **Vim 모션 화면 깜빡임 감소**: synthetic 키스트로크 시 포커스 변경이 없으면 yield 없이 처리되어 motion 도중 깜빡임이 줄어듦
- **원격 워크스페이스에서 터미널이 로컬 home 디렉터리로 잘못 빠지지 않음**: remote project 일 때 home_dir 폴백을 건너뜀
- **symlinked config 디렉터리에서 settings.json 변경 감지**: `~/.config/zed` 가 dotfiles 로 향한 symlink 일 때도 target 측 변경이 정상 전달

---

## v0.4.7 (2026-04-30)

### 정리
- **셸 통합 (OSC 133) 기능 제거**: 설정 → 터미널 → 고급 설정의 "셸 통합" 토글과 관련 부수 기능(탭 명령 결과 아이콘·툴팁의 마지막 명령 상태·실행 중 명령 인자 요약·bash/PowerShell 자동 주입 스크립트) 일괄 삭제. AI CLI 도구별 탭 아이콘과 Claude Code 작업 완료 알림 아이콘은 그대로 유지

### 외부 호환성
- **`Ctrl+↑/↓` 명령 단위 navigation 제거**: prompt mark 점프 단축키가 더 이상 동작하지 않음. 일반 라인 스크롤은 `Shift+↑/↓` 으로 가능
- **`DOKKAEBI_SHELL_INTEGRATION=off` 환경변수 무효화**: 셸 통합 자체가 제거되어 환경변수 escape hatch도 함께 동작 중지

---

## v0.4.6 (2026-04-30)

### UI/UX 개선
- **AI CLI 탭 아이콘 확장**: 터미널 탭에 `claude` 외에 `codex`, `gemini`, `opencode` 실행 중에도 각 서비스 전용 아이콘이 표시되어 어떤 AI CLI 가 돌고 있는지 즉시 식별

---

## v0.4.5 (2026-04-30)

### 새로운 기능
- **터미널 명령 단위 navigation (Ctrl+↑/↓)**: 셸 통합(OSC 133) 활성 시 `Ctrl+↑` 로 직전 프롬프트, `Ctrl+↓` 로 다음 프롬프트 위치로 viewport 즉시 점프. 긴 빌드·테스트 로그에서 명령 단위로 빠르게 이동 가능. 셸 통합 OFF 또는 mark 가 없으면 동작 안 함

### 외부 호환성
- **Ctrl+화살표가 더 이상 셸로 직접 전달되지 않습니다**: bash 의 `forward-word`/`backward-word` (단어 단위 이동) 는 기존 PTY 표준 키인 `Alt+B` / `Alt+F` 또는 `Esc B` / `Esc F` 로 사용 가능. PowerShell PSReadLine 도 동일

---

## v0.4.4 (2026-04-30)

### 새로운 기능
- **새 터미널 탭 색상 자동 부여**: 새 탭 생성 시 워크스페이스 내 다른 탭들과 중복되지 않는 색상이 자동 부여되어 별도 설정 없이도 한눈에 구분 가능. 모든 색상이 사용 중이면 가장 적게 사용된 색상 부여. 사용자가 우클릭 메뉴로 직접 변경한 색상은 그대로 유지

### UI/UX 개선
- **메모장 터미널 메뉴에 색상 표시**: 메모장에서 텍스트 선택 후 우클릭 → 터미널 목록에서도 각 탭의 색상이 좌측 컬러 바로 표시되어 어느 터미널로 보낼지 색으로 즉시 식별

---

## v0.4.3 (2026-04-30)

### 새로운 기능
- **터미널 탭 색상 커스터마이징**: 탭 우클릭 → "탭 색상" 서브메뉴에서 8색(빨강·주황·노랑·초록·파랑·보라·분홍·회색) 또는 "없음" 선택. 지정한 색상은 탭 좌측 얇은 컬러 바로 표시되어 도구 아이콘과 별도로 워크스페이스·원격·용도별 터미널을 시각적으로 구분 가능. 색상은 워크스페이스에 영구 저장되어 재시작 후에도 유지

---

## v0.4.2 (2026-04-29)

### 새로운 기능
- **터미널 셸 통합 (OSC 133)**: bash 와 PowerShell 에서 명령 실행 시작·종료를 인지해 직전 명령의 종료 코드와 실행 상태를 추적. 설정의 터미널 → 고급 설정에서 ON/OFF 토글로 켤 수 있으며 기본값은 OFF. 환경변수 `DOKKAEBI_SHELL_INTEGRATION=off` 로 강제 비활성도 가능. 사용자 dotfiles 는 변경하지 않음

### UI/UX 개선
- **터미널 탭 명령 결과 아이콘**: 일반 셸 탭에서도 마지막 명령의 성공/실패가 탭 아이콘으로 표시되어 백그라운드 빌드·테스트 결과를 한눈에 파악
- **터미널 탭 실행 중 명령 표시**: 명령 실행 중에는 탭 제목이 프로세스명에서 인자가 포함된 명령 라인 요약으로 자동 전환되어 어떤 명령이 돌고 있는지 식별 용이
- **터미널 탭 도구별 전용 아이콘**: claude 실행 중에는 Claude 아이콘, cargo·rustc·clippy·rustup 등 Rust 도구 실행 중에는 Rust 아이콘이 탭에 표시되어 다른 명령과 즉시 구분. 셸 통합 OFF 상태에서도 동작
- **Claude Code 작업 완료 알림 아이콘**: 셸 통합 OFF 상태에서 Claude Code 가 작업 완료를 알리면 탭 아이콘이 작은 점 대신 성공/실패 체크 아이콘으로 직접 표시되어 결과 인지가 명확
- **터미널 탭 툴팁에 명령 상태**: 탭 마우스 오버 시 마지막 명령 결과(성공/실패와 종료 코드) 또는 실행 중 표시가 PID 아래에 추가 노출

---

## v0.4.1 (2026-04-29)

### 새로운 기능
- **프롬프트 팔레트 파라미터 치환**: 등록한 프롬프트에 `{{name}}` 형태의 자리표시자를 두면 호출 시 값 입력 창이 떠서 받은 값으로 치환 후 터미널에 송신. `git commit -m "{{message}}"` 같은 동적 명령 템플릿화 가능

### UI/UX 개선
- **AI 응답의 mermaid 다이어그램 렌더링**: 어시스턴트 응답에 포함된 mermaid 코드 블록이 SVG 다이어그램으로 표시되어 흐름도·시퀀스·간트 차트 등을 시각적으로 확인 가능
- **프롬프트 팔레트 태그 다중 분류**: 프롬프트마다 카테고리 1개 → 태그 다중 부여로 확장. `git` + `branch` 처럼 여러 분류로 묶고 검색에서 모든 태그가 매칭됩니다. 기존 카테고리는 첫 실행 시 태그로 자동 이전(원본 백업 파일 자동 생성)
- **프롬프트 팔레트 사용 빈도 자동 정렬**: 자주 쓰는 항목이 목록 상단으로 자동 이동. 최근 사용 시각 → 누적 사용 횟수 → 등록 순서 우선순위로 정렬되어 검색 입력 없이도 자주 쓰는 명령에 빠르게 접근
- **프롬프트 팔레트 설명 다중 줄 입력**: 프롬프트 등록 시 설명을 여러 줄로 작성 가능하며 줄바꿈이 그대로 저장됩니다. 목록에는 첫 줄만 발췌 표시되어 항목이 길어지지 않습니다
- **프롬프트 등록 모달 설명 미리보기**: 등록/편집 모달의 설명 입력 아래에 markdown 실시간 미리보기 패널 추가. 코드 블록·mermaid 다이어그램이 SVG 로 렌더링되어 작성 중 결과를 즉시 확인 가능

### 정리
- **공개 배포용 라이선스 사본 동봉**: 설치 폴더에 GPL·Apache 라이선스 전문과 NOTICE 사본이 함께 설치되어 오픈소스 라이선스 의무 충족. Windows 파일 속성과 제어판에서도 저작권 표기 확인 가능

### 버그 수정
- **실행 시 화면이 표시되지 않던 좀비 본체 문제 수정**: 워크스페이스 복원이 실패해도 윈도우가 화면에 정상 표시되며, 표시할 윈도우가 하나도 없을 때는 프로세스가 즉시 종료되어 재실행 시 정상 진입
- **인스톨러 자동 실행으로 시작한 본체에서 Claude Code 서브에이전트 뷰 탭이 생성되지 않던 문제 수정**: 인스톨러를 거치지 않고 시작메뉴 더블클릭과 동등한 경로로 본체를 시작하도록 자동 실행 메커니즘을 변경
- **본체 첫 실행 직후 Claude Code 서브에이전트 뷰 탭이 생성되지 않던 문제 수정**: 워크스페이스 mount 완료까지 IPC 처리를 잠시 대기시켜 첫 Task 호출에서도 진행 상황 탭이 정상으로 자동으로 열림
- **서브에이전트 뷰 결과 영역의 줄바꿈 문자(`\n`)가 글자로 표시되던 문제 수정**: 객체 형태의 결과가 화면에서 정상 줄바꿈으로 표시

---

## v0.4.0 (2026-04-25)

### 새로운 기능
- **Zed 최신 v0.233.10 버전까지 업데이트 적용**: [자세한 내용은 공식 Zed 릴리즈 노트 참고](https://github.com/zed-industries/zed/releases/tag/v0.233.10)
- **Claude Opus 4.7 모델 지원**: 언어 모델 선택에 Claude Opus 4.7 이 추가되어 Anthropic·Bedrock·OpenCode 제공자 모두에서 선택 가능. 1M 토큰 컨텍스트와 128K 출력 토큰을 지원
- **워크스페이스 그룹 색상 지정**: 워크스페이스 그룹을 우클릭해 6색 팔레트에서 아이콘 색을 선택할 수 있고, 같은 색을 다시 클릭하면 기본 색상으로 돌아갑니다. 선택한 색상은 앱 재시작 후에도 유지
- **Claude Code 서브에이전트 뷰 탭**: Claude Code 가 Task 도구로 서브에이전트를 실행하면 활성 pane 을 오른쪽/아래쪽으로 split 해 진행 상황 뷰 탭을 자동으로 엽니다. 도구 호출 로그와 최종 응답을 실시간 확인 가능
- **서브에이전트 패널 위치 설정**: 설정 > 알림 > "패널" 섹션의 "서브에이전트 패널 위치" 로 오른쪽(기본)·하단 split 중 선택
- **서브에이전트 뷰 자동 생성 토글**: 설정 > 알림 > "패널" 섹션의 "서브에이전트 뷰 탭" 토글로 자동 생성 제어. 끈 상태에서도 이미 열린 탭은 유지
- **Claude Code 훅 파서(jq) 번들 포함**: Dokkaebi 인스톨러가 `jq.exe`(MIT, jqlang v1.8.1)를 함께 설치해 사용자가 별도 설치 없이 서브에이전트 로그·토스트 본문이 정상 표시됨
- **즐겨찾기 모델에 thinking/effort/fast 설정 저장**: 모델을 즐겨찾기로 등록하면 현재 thinking·effort·fast mode 설정이 함께 기록되고, 해당 모델로 돌아오면 이전 설정이 자동 복원됨
- **모델 설정 토글이 즐겨찾기와 동기화**: thinking / effort 단계 / fast mode 를 UI 에서 바꾸면 기본 모델뿐 아니라 해당 즐겨찾기 항목에도 즉시 반영되어 별도 재설정 불필요
- **앱 재시작 후 fast mode 유지**: 이전에 켜둔 fast mode 가 새 스레드에서도 그대로 유지됨
- **Markdown 프리뷰 헤딩 앵커 링크**: 헤딩에 마우스를 올리면 # 아이콘으로 앵커 URL 을 복사할 수 있고, `[링크](#heading-slug)` 클릭 시 해당 헤딩으로 스크롤
- **Markdown 프리뷰 각주 지원**: `[^1]` 각주 참조를 클릭하면 해당 각주 정의로 이동하고, 정의 안의 되돌아가기 링크로 원래 위치로 복귀
- **OpenAI GPT-5.5 / GPT-5.5 Pro 모델 지원**: OpenAI 제공자에 GPT-5.5 와 GPT-5.5 Pro 가 추가되어 언어 모델 선택에서 바로 사용 가능. 1.05M 토큰 컨텍스트와 128K 출력 토큰을 지원
- **멀티 메모장 (워크스페이스 그룹별 메모 분리)**: 워크스페이스 그룹마다 메모장 내용이 따로 저장되어 그룹을 전환하면 해당 그룹의 메모가 자동으로 표시되고, 그룹 삭제 시 메모도 함께 정리됨. 설정 > 패널 > "메모장 패널" 의 "멀티 메모장" 토글로 켜고 끌 수 있음

### UI/UX 개선
- **에이전트 워크트리 선택기 통합**: 에이전트 패널 툴바의 "현재 워크트리 / 새 워크트리" 2지선다 드롭다운이 통합 워크트리 피커로 교체됨. 현재 브랜치·기본 브랜치·이름 지정 worktree 를 하나의 검색 가능한 팝오버에서 고를 수 있고, 툴바 라벨이 현재 활성 worktree 이름을 바로 보여줌
- **비포커스 창 프레임율 제한으로 에너지 절약**: 다른 창을 보고 있는 동안 Dokkaebi 창의 프레임 갱신을 약 30fps로 제한해 GPU·배터리 소모 감소
- **설정 검색 결과 이동 개선**: 검색 시 가장 많이 일치하는 네비게이션 항목을 자동 선택하고 해당 섹션 헤더로 스크롤해 원하는 설정을 바로 확인 가능
- **환영 탭 최근 프로젝트 이름 풍부화**: 단일 폴더뿐 아니라 여러 루트를 묶은 워크스페이스도 모든 폴더 이름을 표시하고, 원격 프로젝트도 고정 문구 대신 실제 폴더명을 노출
- **삭제된 폴더 최근 프로젝트 1주 유예**: 최근 프로젝트 목록에서 폴더가 사라져도 7일간 항목을 유지해 임시 언마운트된 드라이브·USB를 곧바로 지우지 않음
- **워크스페이스 목록 항목 아이콘**: 좌측 워크스페이스 그룹 패널 각 항목 이름 앞에 터미널 아이콘이 추가되어 워크스페이스가 터미널 묶음임을 한눈에 식별
- **릴리즈 노트 탭 한글화**: 한글 UI에서 릴리즈 노트 탭 이름이 "릴리즈 노트" 로 표시됨. 일반 Markdown 미리보기 탭도 한글에서는 파일 이름만 노출
- **서브에이전트 뷰 세로 스크롤**: 서브에이전트 뷰 탭에 세로 스크롤바가 추가되어 로그가 길어져도 전체 내용 탐색 가능
- **서브에이전트 탭 묶음 배치**: 여러 서브에이전트를 병렬 실행해도 동일한 split pane 안에 탭으로 모여 표시
- **환영 탭 도움말 확장**: 도움말 섹션에 서브에이전트 뷰 탭 활성화 안내가 추가되어 신규 기능 발견성이 향상되고, Claude Code 알림 설정 경로 안내도 실제 설정 메뉴 구조에 맞게 정정됨
- **서브에이전트 뷰 스트리밍 중 스크롤 유지**: 로그가 새로 추가돼도 사용자가 보고 있던 위치와 드래그 선택이 리셋되지 않고 꼬리에만 증분 반영
- **서브에이전트 뷰 로그 정확도**: 동일 세션에서 다른 서브에이전트나 부모 에이전트의 메시지가 뷰에 섞여 나오던 경로를 차단해 자기 로그만 표시

### 보안
- **공급망 취약점 전량 해결**: openssl(HIGH 4 + LOW 1) · rand(LOW 1) · rustls-webpki(LOW 2) 총 8건의 Dependabot 알림을 lockfile 갱신 및 AWS SDK feature 조정으로 해결하고 내부 TLS 스택을 rustls 0.23 단일 경로로 정리

### 정리
- **Claude Code 훅 처리 지연 단축**: 서브에이전트 훅에서 호출하던 외부 파서(jq) 프로세스 수를 훅당 5회에서 1회로 통합해 훅 응답 속도 개선
- **transcript 자동 정리 시작 지연 완화**: 앱 시작 시 24시간 이내 이미 정리한 경우 재스캔을 건너뛰어 부팅 I/O 중복 제거
- **HTTP MCP 컨텍스트 서버 설정 자동 정리**: HTTP/SSE 형 MCP 컨텍스트 서버에서 더 이상 사용되지 않는 `settings` 키가 마이그레이션 시 자동으로 제거됨

### 버그 수정
- **업그레이드 후 누락된 에이전트 스레드 복원**: 저장된 legacy 스레드가 사이드바에 다시 표시되도록 마이그레이션 타이밍 문제를 수정
- **임시 버퍼 워크스페이스 재시작 후 복원**: 저장하지 않은 임시 버퍼만 있는 워크스페이스가 재시작 시 사라지지 않도록 세션 복원 동작을 개선
- **구형·가상 GPU 기동 크래시 방지**: Bgra8Unorm 텍스처 포맷을 지원하지 않는 GPU 에서 Dokkaebi 가 panic 없이 Rgba8Unorm 으로 자동 전환되어 정상 실행됨 (일부 가상화·원격 데스크톱·구형 드라이버 환경)
- **AI 편집 커밋 레이스 수정**: 에이전트 편집을 git 으로 커밋하는 동시에 사용자가 다른 곳을 편집해도 에이전트 편집이 정상적으로 "수락됨" 으로 처리되고 리뷰 대기 목록에서 사라짐
- **tsgo LSP 수정**: TypeScript Go 포트(tsgo) 언어 서버가 정상 동작하도록 lsp-types 를 갱신하고, LSP 직렬화의 null / 빈 필드 처리 호환성도 함께 개선
- **Flex dock 너비 안정화**: 좌/우 dock 을 flexible 크기로 설정한 상태에서 센터 영역을 좌우로 split 하거나 활성 pane 을 이동해도 dock 비율이 흔들리지 않고 센터 열 수에 비례한 값으로 유지됨
- **Windows 앱 종료 시 크래시 방지**: Tokio 런타임 종료 경로에서 확장 작업이 panic을 일으키던 문제를 수정해 Dokkaebi를 닫을 때 비정상 종료가 발생하지 않음
- **Windows Git 상태 최신화**: 파일 변경 후에도 상태 패널과 변경 표시가 오래된 상태로 남던 문제를 수정해 Git 상태가 즉시 반영됨
- **Git 오류 토스트 크래시 방지**: 워크트리 생성 실패 등 Git 오류 알림이 뜨는 경로에서 발생하던 중복 대여 크래시를 수정
- **Git Graph 브랜치 변경 실시간 반영**: 브랜치를 새로 만들거나 지워도 HEAD 이동 없이 Git Graph 가 즉시 갱신됨
- **Git Graph stash 변경 실시간 반영**: `git stash push`/`pop` 이 Git Graph 에 바로 반영되어 stash 기록을 즉시 확인 가능
- **Markdown 프리뷰 밑줄·취소선 잔상 제거**: 프리뷰가 작은 크기로 축소돼 글자가 줄바꿈될 때 마지막 글자 뒤로 밑줄·취소선이 삐져나오던 문제 수정
- **단어 이동으로 접힘·@mention 건너뛰기**: Ctrl+좌/우 단어 이동이 접힌 코드 영역과 `@mention` 칩을 넘어가지 못하고 경계에 붙어버리던 문제 수정
- **테마 변경 시 브레드크럼 색상 즉시 갱신**: 테마를 전환해도 파일 경로 브레드크럼의 문법 색상이 이전 테마 값 그대로 남던 문제 수정
- **이름 변경한 파일 재시작 후 경로 유지**: 열려 있는 파일을 rename 하고 앱을 재시작하면 이전 경로로 열려 오류가 나던 문제 수정
- **활성 탭 마지막 탭 닫을 때 탭 전환기 유지**: 활성 pane의 마지막 탭을 닫을 때 탭 전환기(Ctrl+Tab)가 사라져 다른 탭으로 이동할 수 없던 문제 수정
- **정규식 검색 중 비매칭 강조 억제**: 정규식 검색 결과가 선택된 상태에서 regex 와 일치하지 않는 단어까지 occurrence 하이라이트가 겹쳐 표시되던 문제 수정
- **멀티버퍼 검색 결과 버퍼 semantic 하이라이팅**: 검색 결과에서 단일 파일을 열 때 semantic 토큰 하이라이팅이 초기 로드되지 않던 문제 수정
- **파일 복제 시 전체 이름 선택**: 파일을 복제해 rename 모드로 들어가면 `' copy'` 접미사만 선택되던 것을 파일명 전체 선택으로 변경해 새 이름을 바로 타이핑 가능
- **`-` 시작 경로 Git 명령 실패 방지**: `-`로 시작하는 파일명이 있는 저장소에서 blame·stage·unstage·stash·status가 옵션으로 오인되어 실패할 수 있던 문제 수정
- **ACP 서버 사용자 메시지 중복 방지**: opencode 등 일부 ACP 서버가 사용자 프롬프트 chunk를 에코해 동일 내용이 두 번 표시되던 문제 수정
- **에이전트 스레드 중복 세션 방지**: 동일 스레드를 연속으로 열면 같은 세션이 두 개 생성되던 문제 수정
- **Anthropic 커스텀 모델 thinking 모드 보존**: 사용자가 custom 모델에 설정한 thinking/adaptive-thinking 모드가 내장 모델 기준으로 덮어씌워지던 문제 수정
- **에이전트 도구 호출 포커스 유지**: 에이전트 패널을 스크롤해 도구 호출이 화면 밖으로 나가도 포커스 상태가 유지되도록 수정
- **제목 생성 실패 시 재시도 버튼**: 스레드 제목 자동 생성이 실패하면 타이틀 옆에 오류 아이콘이 표시되고 클릭해 재시도 가능
- **ACP 서버 종료 시 크래시 방지**: ACP 프로세스가 비정상 종료될 때 여러 세션을 순회하며 이벤트를 발생시키다가 중복 대여로 크래시 나던 문제 수정
- **에이전트 패널 최소 너비 보장**: 좌/우 도크에 고정된 에이전트 패널이 300px 미만으로 리사이즈되지 않도록 제한
- **큐 메시지 삭제 후 입력창 자동 포커스**: 대기 중인 메시지를 취소하면 다음 입력이 바로 가능하도록 메시지 입력창에 포커스 이동
- **Ollama 사용자 설정 컨텍스트 우선 적용**: 모델에 `num_ctx` 파라미터가 지정돼 있으면 모델 아키텍처 기본 컨텍스트보다 우선 사용
- **Git 패널 트리 Enter 키로 폴더 접기/펼치기**: 트리 뷰에서 디렉터리를 선택하고 Enter를 누르면 diff 대신 접기/펼치기 동작
- **Git diff hunk staging 동시성 수정**: Uncommitted diff가 Unstaged diff보다 먼저 열릴 때 staging 상태가 잘못 표시되던 경합 수정
- **Bare 저장소 경로 최근 프로젝트 보존**: `.bare` 디렉터리를 쓰는 워크트리 레이아웃에서 최근 프로젝트 경로가 사라지던 문제 수정
- **Git 명령 출력 ANSI 이스케이프 제거**: Git 실행 결과 창에 색 코드·진행 표시가 원시 `\x1b[...` 형태로 노출되던 문제 수정
- **TopoJSON 파일 문법 하이라이팅**: `.topojson` 확장자 파일이 JSON 문법으로 색상 표시되도록 자동 감지

### 새로운 기능 (Phase 3~5 백포트 추가분)
- **Ctrl+마우스 휠 에디터 폰트 크기 조정**: 에디터에서 Ctrl을 누른 채 휠을 굴려 폰트 크기 즉시 변경 가능
- **hover 팝업 고정·숨김 지연 설정**: `hover_popover_sticky`·`hover_popover_hiding_delay`로 hover 박스가 마우스 접근 시 유지되는 동작 제어
- **PNM(Netpbm) 이미지 프리뷰 지원**: `.pbm`·`.ppm`·`.pgm` 이미지 파일이 에디터에서 바로 열림
- **Fix with Assistant 코드 액션 제거**: 진단 컨텍스트에서 뜨던 "Fix with Assistant" 항목 제거, `assistant: inline assist` 액션은 그대로 유지
- **Format and Save 액션 추가**: `workspace: format and save` 액션으로 format_on_save 설정과 무관하게 항상 포맷 후 저장 가능
- **블록 주석 토글 단축키**: `Ctrl+K Ctrl+/` 또는 `Shift+Alt+A` 로 선택 영역을 `/* ... */` 같은 블록 주석으로 감싸거나 해제 (C/C++/Go/JS/JSONC/Markdown/Python/Rust/TSX 지원)
- **Git Graph 현재 브랜치 강조**: 커밋 옆 브랜치 칩 중 현재 체크아웃한 브랜치는 체크 아이콘 + 진한 배경으로 강조 표시. 긴 브랜치 이름은 자동 말줄임 처리로 폭 넘김 방지

---

## v0.3.0 (2026-04-21)

### 새로운 기능
- **Windows 자동 실행 설정**: 설정 > 일반의 "자동 실행" 토글로 Windows 로그인 시 Dokkaebi 자동 실행을 켜고 끌 수 있음. 활성화하면 다음 로그인부터 자동으로 실행되고, 비활성화하거나 앱을 제거하면 자동 실행 등록이 해제됨
- **Claude Code 작업 알림**: Claude Code의 작업 완료·입력 대기·권한 요청을 Dokkaebi 토스트 알림으로 표시. 토스트 본문에 "그룹 / 탭" 라인이 함께 노출되어 발신 위치 식별 가능
- **터미널 탭/그룹 인디케이터**: 알림 도착 터미널 탭에는 dot, 해당 터미널이 속한 비활성 워크스페이스 그룹에는 배지 표시
- **알림 플러그인 관리**: 설정 > 알림 > Claude Code의 [설치]/[제거] 버튼으로 활성화 제어. 인스톨러에 번들되어 별도 다운로드 불필요

### UI/UX 개선
- **알림 메뉴 재구성**: "Claude Code 작업 완료 알림음" 항목이 "Claude Code 플러그인" + "작업 알림" 두 항목으로 재구성되어 플러그인 설치 상태가 화면에 즉시 표시됨
- **토스트 자동 사라짐 시간 조정**: 작업 완료·입력 대기 토스트는 기본 5초 후 자동으로 사라지고, 설정 > 알림 > "토스트 팝업" 섹션에서 5~300초 범위 조정 가능. 권한 요청 토스트는 사용자가 닫을 때까지 유지
- **토스트 중첩 방지**: 입력 대기 토스트가 떠 있을 때 새 작업 완료 알림이 도착하면 이전 토스트는 자동 정리. 여러 창에 같은 이름 탭이 있어도 발신 터미널이 속한 창에만 토스트 표시
- **Claude Code 토스트 내용 풍부화**: 작업 완료 시 직전 프롬프트와 응답 요약을 `"프롬프트 → 응답"` 형식으로, 권한 요청 시 도구 이름과 실행할 명령·파일 preview를, 입력 대기 시 Claude가 띄운 실제 질문을 토스트에서 바로 확인 가능
- **토스트 다국어 적용**: 토스트 제목·베이스 문구가 설정된 UI 언어(한국어/English)에 맞춰 즉시 전환
- **CLI 바이너리 아이콘 적용**: 설치 폴더와 탐색기에서 `dokkaebi-cli.exe`도 메인 앱과 동일한 도깨비 아이콘으로 표시되어 비주얼 일관성 향상
- **워크스페이스 그룹 패널 항목 확대**: 좌측 워크스페이스 그룹 패널의 항목 세로 높이를 약 30% 크게 조정해 클릭 영역과 가독성 개선
- **메모장 기본 복원 켜짐**: 설정 > 패널 > 메모장 복원 기본값이 켜짐으로 바뀌어 작성 중이던 메모가 앱 재시작 후에도 자동으로 유지됨

### 보안
- **rustls-webpki 보안 패치**: Dependabot 보안 경고 2건 해결을 위해 `rustls-webpki`를 0.103.11 → 0.103.12로 업데이트 (TLS 인증서 검증 관련 low 등급 경고 2건)

### 정리
- **마커 파일 기반 알림 시스템 폐기**: 기존 "Claude Code 작업 완료 알림음"이 사용하던 임시 파일 폴링 방식을 제거하고 IPC 기반 알림으로 전환. 기존 사용자 환경의 잔여 hook은 부팅 시 1회 자동으로 정리됨
- **언인스톨 시 Claude Code 훅 자동 정리**: 인스톨러로 Dokkaebi를 제거하면 `~/.claude/settings.json`에 등록된 Dokkaebi 플러그인 항목이 함께 제거됨. 사용자가 편집한 다른 Claude Code 설정은 보존되고, 자동 업데이트 재설치 경로에서는 hook을 유지해 업데이트 후 재등록 불필요

### 버그 수정
- **Copilot Chat reasoning effort 전송 오류 수정**: Copilot proxy가 검증을 강화하면서 reasoning effort가 지정되지 않은 상태로 메시지를 보내면 거부되던 문제 수정 (상류 Zed v0.232.3 반영)
     ([자세한 내용은 공식 Zed 릴리즈 노트 참고](https://github.com/zed-industries/zed/releases/tag/v0.232.3))

---

## v0.2.0 (2026-04-18)

### 새로운 기능
- **업스트림 Zed v0.232.2 백포트**: ESLint 3.0.24, LanguageAwareStyling, ACP slash commands 복원, Markdown HTML 정렬, 프로젝트 패널 정렬 순서, SSH nickname 표시, 이미지 멘션 이름, OpenAI reasoning_effort, JSX 컴포넌트 하이라이팅, 카드 레이아웃 패딩, 병합 충돌 상태바 표시기, SVG 폰트 fallback 등 20+ PR 이식
  ([자세한 내용은 공식 Zed 릴리즈 노트 참고](https://github.com/zed-industries/zed/releases/tag/v0.232.2))
- **릴리즈 노트 메뉴**: 설정 메뉴에서 릴리즈 노트를 직접 확인 가능
- **메모장 패널 단축키**: `Ctrl+Shift+M`으로 메모장 패널 토글
- **UI 언어 변경 UI**: 설정 > 일반에서 언어(시스템 언어 / English / 한국어) 선택 가능. 시스템 언어가 기본값이며 OS 언어를 자동 감지하고, 변경 시 재시작 없이 즉시 적용
- **자동 업데이트 후 릴리즈 노트 표시**: 앱 내부 자동 업데이트로 새 버전이 설치되면 설치가 완료되는 즉시 앱이 자동 재실행되고 릴리즈 노트가 1회 자동으로 열림. 설정 > 일반 > 자동 업데이트의 "릴리즈 노트" 토글로 켜고 끌 수 있음

### UI/UX 개선
- **대규모 i18n 한글화**: 다이얼로그, 툴팁, 버튼, 라벨, 피커 placeholder, 드롭다운, 확장 카드, 키맵 편집기, 상태바 LSP 버튼, ETW 트레이싱 알림 등 100+ 문자열 한글 적용
- **워크스페이스 그룹 패널**: 항목 간 4px 간격 추가로 가독성 향상
- **프로젝트 패널 정렬**: 설정 UI에서 정렬 모드/순서를 직관적인 위치로 이동
- **Windows 앱 목록 표시명 단순화**: "Dokkaebi 0.2.0" → "Dokkaebi"로 표시. 버전은 별도 줄(`DisplayVersion`)에 그대로 노출
- **정보(About) 팝업 리뉴얼**: 상류 Zed 스타일의 독립 윈도우로 전환하고 상단에 앱 아이콘을 추가. 타이틀·버전·크레딧·확인 버튼은 기존 내용 유지. 듀얼 모니터 환경에서는 현재 Dokkaebi 메인 앱이 있는 모니터 중앙에 표시

### 보안
- **wasmtime 33 → 36 업그레이드**: Dependabot 보안 경고 해결 (critical 2, moderate 10, low 3)
- **설치 다운그레이드 보호**: 이미 설치된 버전이 더 높으면 설치 시 경고 후 진행 여부 확인. 앱 자동 업데이트 경로(silent)에서는 다운그레이드를 무조건 차단하여 의도치 않은 버전 되돌림 방지

### 정리
- **REPL/Notebook 크레이트 완전 제거**: Windows 환경에서 사용 불가한 Jupyter 관련 코드 정리
- **설정 메뉴 정리**: 불필요한 키맵 파일 메뉴 항목 제거

### 버그 수정
- Non-ASCII 아카이브 스레드 제목 크래시 수정 (한국어 관련)
- 프로젝트 심볼 UTF-8 패닉 수정
- 설정 입력 필드 blur 시 데이터 손실 수정
- Hover popover delay 설정 경로 수정
- 프로필 선택기 Shift+Tab 재렌더링 수정
- Rules library 호버 시 활성 규칙 유지

---

## v0.1.1 (2026-04-15)

### 새로운 기능
- **터미널/셸 UX 개선**: 성능 최적화 및 그룹 패널 간격 조정
- **Claude Code 알림 훅**: 설정 UI 조건 추가 및 컴파일 경고 제거

### 정리
- Dead/미지원 설정 옵션 정리
- i18n 고아 키 정리

---

## v0.1.0 (2026-04-12 ~ 2026-04-14)

### 핵심 변경
- **Zed → Dokkaebi 포크**: Windows 전용 에디터로 재구성
- **portable-pty 마이그레이션**: alacritty EventLoop에서 portable-pty로 터미널 백엔드 교체
- **전체 UI 한글화**: 메뉴, 설정, 액션 이름, 상태 메시지 등 한글 기본 적용
- **라이선스/상류 잔재 정리**: Zed SaaS ToS, cloud 워크플로우, Nix/Docker 빌드 등 제거
- **Windows DPI 처리**: 다중 모니터 환경에서 SetWindowPlacement 좌표계 분리
