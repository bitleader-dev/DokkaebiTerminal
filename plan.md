# 워크스페이스 그룹 패널 항목 세로 높이 30% 확대

## 목표
- 좌측 워크스페이스 그룹 패널의 그룹 항목(`워크스페이스 1`, `워크스페이스 2` 등) 세로 높이를 현재 대비 약 30% 크게 만들어 클릭 영역과 가독성을 개선.

## 현재 상태
- `crates/workspace/src/workspace_group_panel.rs:984` — 그룹 항목 컨테이너 `h_flex()`에 `.py(px(4.))` 적용 중.
- 항목 내부 텍스트는 `text_sm`(≈14px line-height). 상·하 패딩 각 4px 포함 총 높이 ≈ 22~24px.

## 작업 범위
### 수정 파일 1개
1. **`crates/workspace/src/workspace_group_panel.rs`**
   - Line 984 `.py(px(4.))` → `.py(px(8.))`
   - 상·하 패딩을 4px → 8px로 증가 → 항목 총 높이 약 +8px → 기존 ≈22~24px → ≈30~32px (약 +30%)

### 영향 범위
- 워크스페이스 그룹 패널의 그룹 목록 항목 세로 크기만 증가.
- 인라인 이름 편집기의 내부 패딩(`py(px(1.))`)은 건드리지 않음 → 편집 모드에서도 외곽 높이만 함께 커짐(일관성 유지).
- 헤더(`+` 버튼이 있는 상단 바)는 영향 없음.

## 승인 필요 사항 (CLAUDE.md 1단계)
- UI 세부 수치(여백 값) 1개 수정. 구조·API·DB·의존성 변경 없음.
- 기존 동작 유지, 단순 크기 조정.
- **승인 요청**: `py(px(4.))` → `py(px(8.))` 로 변경하는 방향이 맞는지 확인 부탁드립니다. (다른 값 원하시면 수치 지정해주세요.)

## 검증
1. `cargo check -p workspace` → 신규 경고/에러 0건.
2. 실제 빌드·실행해서 사이드바 항목 높이가 시각적으로 약 30% 커졌는지 확인.

## 작업 단계
- [x] 1. `workspace_group_panel.rs:984` py 값 변경
- [x] 2. `cargo check -p workspace` 통과 확인 (1m 39s, 신규 경고/에러 0건)
- [x] 3. `notes.md` 갱신
- [x] 4. `release_notes.md` v0.3.0 UI/UX 개선에 항목 추가
